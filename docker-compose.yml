services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-modelar}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-modelar}
      POSTGRES_DB: ${POSTGRES_DB:-modelar}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-modelar}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
